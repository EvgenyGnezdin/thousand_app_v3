type settingProp = {
    dots: boolean,
    slidesToShow: number,
    infinite: boolean,
    slidesToScroll:number,
    speed: number 
}

export  const settingSlider: settingProp = {
    dots: false,
    slidesToShow: 1,
    infinite: true,
    slidesToScroll: 1,
    speed: 0,
  };